<div align="center">
  <p style="color: green"><%= notice %></p>
  <div class="movies">
    <br>
    <h1 align="center">Movies</h1>
  </div>
  <%= form_tag(movies_path, method: 'get') do %>
    <%= text_field_tag :search, params[:search], placeholder: 'Search by movie name'%>
    <%= submit_tag 'Search', class: "btn btn-secondary btn-sm" %>
    <%= link_to 'Clear', root_path, class: "btn btn-secondary btn-sm" %>
  <% end %>
<br>
  <%= form_tag movies_path, method: :get do %>
    <div align="center">
      <%= label_tag 'from_date', 'From Date:', style: 'color: white;' %>
      <%= date_field_tag 'from_date', params[:from_date], class: 'datepicker'%>

      <%= label_tag 'to_date', 'To Date:', style: 'color: white;' %>
      <%= date_field_tag 'to_date', params[:to_date], class: 'datepicker'%>
      <%= submit_tag 'Apply Filter', class: "btn btn-secondary btn-sm" %>
      <%= link_to 'Clear filter', root_path, class: "btn btn-secondary btn-sm" %>
  <% end %>
  <div id="movies" class="clearfix">
    <% @movies.each do |movie| %>
      <%= render movie %>
    <% end %>
  </div>

  <% if current_user.id==1 %>
    <br>
    <%= link_to "Add new movie", new_movie_path, class: "btn btn-info  btn-block" %>
  <% end %>
</div>
</div>
<style>

.clearfix::after {
content: "";
display: table;
clear: both;
}

.movies
 {
     color: #F7F7F7;

 }

</style>